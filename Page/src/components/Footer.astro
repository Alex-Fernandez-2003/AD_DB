---
const footerLinks = [
  {
    title: "Recursos",
    links: ["Documentación", "Soporte", "Blog"]
  },
  {
    title: "Comunidad",
    links: ["Eventos", "Testimonios", "Casos de Éxito", "Contáctanos"]
  },
  {
    title: "Sobre nosotros",
    links: ["Política de Privacidad", "Términos de Servicio", "Configuración de Cookies", "Suscríbete"]
  }
];

const socialIcons = ["facebook", "instagram", "twitter", "linkedin", "youtube"];
---
<footer class="bg-gray-900 text-white py-20 px-4">
  <div class="container mx-auto">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-12 mb-12">
      <div>
        <h3 class="text-xl font-semibold mb-4">Equipo SQL Monitor</h3>
      </div>
      {footerLinks.map((column) => (
        <div>
          <h3 class="text-xl font-semibold mb-4">{column.title}</h3>
          <ul class="space-y-2">
            {column.links.map((link) => (
              <li><a href="#" class="hover:text-purple-400 transition duration-300">{link}</a></li>
            ))}
          </ul>
        </div>
      ))}
    </div>
    <div class="border-t border-gray-700 pt-12">
      <div class="flex flex-col md:flex-row justify-between items-center">
        <form class="w-full md:w-auto mb-8 md:mb-0">
          <h3 class="text-xl font-semibold mb-4">Únete a nosotros para recibir actualizaciones.</h3>
          <div class="flex flex-col md:flex-row gap-4">
            <input 
              type="email" 
              placeholder="Introduce tu correo electrónico" 
              class="bg-black border border-gray-700 rounded-lg px-4 py-2 flex-grow"
            />
            <button class="bg-purple-600 text-white px-6 py-2 rounded-lg hover:bg-purple-700 transition duration-300">
              Suscribirse
            </button>
          </div>
          <p class="text-sm text-gray-400 mt-2">Al suscribirte, aceptas nuestra Política de Privacidad.</p>
        </form>
        <div class="flex gap-4">
          {socialIcons.map((icon) => (
            <a href="#" class="text-gray-400 hover:text-white transition duration-300">
              <img src={`/icon-${icon}.svg`} alt={`${icon} icon`} class="w-6 h-6" />
            </a>
          ))}
        </div>
      </div>
    </div>
    <div class="border-t border-gray-700 mt-12 pt-8 text-center text-sm text-gray-400">
      <p>© 2024 SQL Monitor. Todos los derechos reservados.</p>
      <div class="flex justify-center gap-4 mt-4">
        <a href="#" class="hover:text-white transition duration-300">Política de Privacidad</a>
        <a href="#" class="hover:text-white transition duration-300">Términos de Servicio</a>
        <a href="#" class="hover:text-white transition duration-300">Configuración de Cookies</a>
      </div>
    </div>
  </div>
</footer>